# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    article[title]:
        label: Title
    template:
        label: Template
    index:
        label: Index
